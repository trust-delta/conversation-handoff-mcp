{
  "name": "conversation-handoff-mcp",
  "version": "0.14.0",
  "bin": {
    "conversation-handoff-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "description": "MCP server for conversation handoff between AI chats/projects",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build:ui": "INPUT=ui/viewer.html vite build",
    "build": "npm run build:ui && tsc",
    "clean": "rimraf dist",
    "rebuild": "npm run clean && npm run build",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "lint": "biome lint src/",
    "lint:fix": "biome lint --write src/",
    "format": "biome format --write src/",
    "format:check": "biome format src/",
    "check": "biome check src/",
    "check:fix": "biome check --write src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "prepublishOnly": "npm run check && npm run test && npm run rebuild",
    "prepare": "husky"
  },
  "keywords": [
    "mcp",
    "claude",
    "handoff",
    "conversation",
    "ai",
    "context",
    "model-context-protocol"
  ],
  "author": "trust-delta",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/trust-delta/conversation-handoff-mcp.git"
  },
  "homepage": "https://github.com/trust-delta/conversation-handoff-mcp#readme",
  "bugs": {
    "url": "https://github.com/trust-delta/conversation-handoff-mcp/issues"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/ext-apps": "^1.7.5",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.11",
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^4.1.11",
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "rimraf": "^6.0.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vite-plugin-singlefile": "^2.3.0",
    "vitest": "^4.1.11"
  },
  "lint-staged": {
    "src/**/*.ts": [
      "biome check --write"
    ]
  }
}
